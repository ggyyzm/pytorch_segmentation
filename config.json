{
    "name": "PSPNet",
    "n_gpu": 1,
    "use_synch_bn": true,

    "arch": {
        "type": "PSPNet",
        "args": {
            "backbone": "resnet50",
            "freeze_bn": false,
            "in_channels": 3
        }
    },

    "train_loader": {
        "type": "Salinas",
        "args":{
            "data_dir": "data/salinas_seg",
            "batch_size": 8,
            "base_size": 512,
            "crop_size": false,
            "augment": true,
            "shuffle": true,
            "scale": false,
            "flip": true,
            "rotate": false,
            "blur": false,
            "split": "trainval",
            "num_workers": 8
        }
    },

    "val_loader": {
        "type": "Salinas",
        "args":{
            "data_dir": "data/salinas_seg",
            "batch_size": 1,
            "crop_size": false,
            "val": true,
            "split": "val",
            "num_workers": 0
        }
    },

    "optimizer": {
        "type": "SGD",
        "differential_lr": true,
        "args":{
            "lr": 0.01,
            "weight_decay": 1e-4,
            "momentum": 0.9
        }
    },

    "loss": "CrossEntropyLoss2d",
    "ignore_index": 0,
    "lr_scheduler": {
        "type": "Poly",
        "args": {}
    },

    "trainer": {
        "epochs": 80,
        "save_dir": "saved/",
        "save_period": 10,
  
        "monitor": "max Mean_IoU",
        "early_stop": 10,
        
        "tensorboard": true,
        "log_dir": "saved/runs",
        "log_per_iter": 20,

        "val": true,
        "val_per_epochs": 5
    }
}
